{
  "PostToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "command",
          "command": "ruff check --fix || true"
        },
        {
          "type": "command",
          "command": "ruff format || true"
        }
      ]
    }
  ]
}
