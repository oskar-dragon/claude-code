{
  "name": "@oskar-dragon/ccf",
  "version": "0.1.0",
  "description": "CLI tool for Claude Code Flow",
  "module": "cli/src/index.ts",
  "type": "module",
  "bin": {
    "ccf": "./cli/dist/index.js"
  },
  "scripts": {
    "build": "bun build cli/src/index.ts --outdir cli/dist --target bun --format esm",
    "build:macos-arm64": "bun build --compile --minify --target=bun-darwin-arm64 cli/src/index.ts --outfile cli/bin/ccf-macos-arm64",
    "build:macos-x64": "bun build --compile --minify --target=bun-darwin-x64 cli/src/index.ts --outfile cli/bin/ccf-macos-x64",
    "build:linux-x64": "bun build --compile --minify --target=bun-linux-x64 cli/src/index.ts --outfile cli/bin/ccf-linux-x64",
    "build:all": "bun run build:macos-arm64 && bun run build:macos-x64 && bun run build:linux-x64",
    "dev": "bun run cli/src/index.ts",
    "prepublishOnly": "bun run build",
    "typecheck": "bunx tsc --noEmit"
  },
  "keywords": [
    "claude-code",
    "project-management",
    "cli"
  ],
  "author": "Oskar Dragon",
  "license": "MIT",
  "files": [
    "cli/dist"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@stricli/core": "^1.2.0"
  }
}
